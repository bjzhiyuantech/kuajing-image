{
  "name": "gpt-image-canvas",
  "version": "0.1.1",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@9.14.2",
  "description": "Local professional AI canvas powered by tldraw and GPT Image 2.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"corepack pnpm api:dev\" \"corepack pnpm web:dev\"",
    "api:dev": "corepack pnpm --filter @gpt-image-canvas/api dev",
    "web:dev": "corepack pnpm --filter @gpt-image-canvas/web dev",
    "build": "corepack pnpm --filter @gpt-image-canvas/shared build && corepack pnpm --filter @gpt-image-canvas/web build && corepack pnpm --filter @gpt-image-canvas/api build && corepack pnpm --filter @gpt-image-canvas/extension build && corepack pnpm --filter @gpt-image-canvas/desktop build",
    "deployment:gates": "corepack pnpm --filter @gpt-image-canvas/api check:deployment-gates",
    "deployment:backup": "node scripts/deployment-backup.mjs",
    "deployment:bundle": "node scripts/prepare-deployment-bundle.mjs",
    "deployment:images": "node scripts/deployment-images.mjs",
    "deployment:preflight": "node scripts/deployment-preflight.mjs",
    "deployment:rollout": "node scripts/deployment-rollout.mjs",
    "deployment:smoke": "node scripts/post-deploy-smoke.mjs",
    "desktop:build": "corepack pnpm --filter @gpt-image-canvas/desktop build",
    "desktop:dist": "node scripts/package-desktop.mjs",
    "mirror:tldraw-assets": "node scripts/mirror-tldraw-assets.mjs",
    "standalone:package": "node scripts/package-standalone.mjs",
    "typecheck": "corepack pnpm --filter @gpt-image-canvas/shared typecheck && corepack pnpm --filter @gpt-image-canvas/web typecheck && corepack pnpm --filter @gpt-image-canvas/api typecheck && corepack pnpm --filter @gpt-image-canvas/extension typecheck && corepack pnpm --filter @gpt-image-canvas/desktop typecheck",
    "start": "corepack pnpm --filter @gpt-image-canvas/api start"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
